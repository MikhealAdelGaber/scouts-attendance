<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Angular SPA" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <!-- Skip requests for real files (js, css, images, etc.) -->
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <!-- Skip requests for real directories -->
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
            <!-- Skip API requests -->
            <add input="{REQUEST_URI}" pattern="^/api" negate="true" />
            <!-- Skip SignalR hub requests -->
            <add input="{REQUEST_URI}" pattern="^/hubs" negate="true" />
            <!-- Skip Swagger -->
            <add input="{REQUEST_URI}" pattern="^/swagger" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>
    <staticContent>
      <mimeMap fileExtension=".webmanifest" mimeType="application/manifest+json" />
    </staticContent>
  </system.webServer>
</configuration>
